# HomeBox 项目全面扫描与审计指令

## 目标

请你从头到尾完整阅读 `D:/home-storage/src/` 目录下的 **所有 TypeScript/TSX 文件**，然后给我一份详尽的审计报告。报告需包含以下 3 个核心部分：

---

## 第一部分：当前项目全景

请列出项目的完整架构，包括：

### 技术栈
- 前端框架、路由方案、状态管理、构建工具
- UI 设计体系（设计语言、配色、组件库）
- 后端服务（数据库、认证、存储）
- 使用的 npm 依赖（区分核心依赖和辅助依赖）
- 部署平台和方式

### 数据模型
- 核心数据实体（Item, Location, FloorPlan）的字段结构
- 数据库表结构（根据 `src/services/db.ts` 推断）
- 数据流：数据从 Supabase 到 Zustand 到 UI 组件的完整路径

### 功能模块清单
逐页列出每个页面/组件的功能，包括：
- `src/pages/` 下每个页面（Home, Items, ItemForm, Locations, FloorPlan, BatchManage, Settings, AuthPage）
- `src/components/` 下每个组件（Layout, AIChat, FamilyModal, AppLock, Toast, PinPad, QRCodeGenerator, ExpirationWarning, ThemeToggle, PrivacyPolicyModal, ConfirmDialog, ErrorBoundary）
- `src/components/ui/` 下每个 UI 基础组件（Button, ButtonLink, Card, Input, Select, Modal, Badge, IconButton, Spinner）
- `src/services/` 下每个服务模块的功能范围
- `src/store/useStore.ts` 的状态结构和核心逻辑

---

## 第二部分：未来可扩展功能

请基于当前架构和数据模型，提出**具体可行的功能扩展建议**。每个建议需包含：

1. **功能名称和一句话描述**
2. **价值**：为什么这个功能对用户有价值
3. **可行性**：基于现有架构，实现难度如何
4. **建议的存放位置**：放在哪个现有文件/目录中，或是否需要新建文件

至少列出 8-10 个有实质价值的扩展建议。可以涉及：
- 新的页面或模块
- 增强现有功能
- 数据分析和洞察
- 用户体验改进
- 国际化/本地化
- 第三方集成

---

## 第三部分：当前弱点和改进建议

请基于代码扫描，指出项目中**确实存在的问题**，而不是泛泛而谈。重点关注：

### 代码质量
- 文件是否过大、职责是否单一
- 是否有重复代码或反模式
- 是否存在 any 类型或类型安全漏洞
- 错误处理是否到位
- 注释是否过多或过少

### 架构问题
- 组件拆分是否合理
- 数据流是否清晰
- 是否有循环依赖或耦合过紧

### 功能缺失
- 边界情况是否未处理（空状态、加载态、错误态）
- 用户反馈是否充分
- 数据验证是否缺乏

### 安全与性能
- 是否存在安全问题（如 PIN 码存储方式）
- 是否有性能隐患（不必要的重渲染、大数据量处理）
- PWA 配置是否合理

### 测试
- 测试覆盖情况
- 哪些核心逻辑最需要测试

---

## 输出要求

- 使用 Markdown 格式
- 对每个问题，请指出**具体的文件路径和行号范围**
- 每个建议请给出**为什么**（分析）而不是只给结论
- 严禁泛泛而谈，所有判断必须有代码依据
- 第二部分和第三部分请各自给出明确的优先级（P0/P1/P2）
